# Install into a virtual environment:
#   python -m venv .venv
#   .venv\Scripts\activate           (Windows)   |   source .venv/bin/activate (mac/linux)
#   pip install -r requirements.txt

# --- LangChain 1.x (orchestration: LCEL, retrievers, memory, RAG chains) ---
# Pinned: in 1.x the legacy RAG helper chains live in `langchain-classic`,
# and HuggingFaceCrossEncoder lives in `langchain-community.cross_encoders`.
langchain>=1.3,<2
langchain-classic>=1.0,<2           # create_retrieval_chain, EnsembleRetriever, reranker
langchain-community>=0.4,<1
langchain-google-genai>=4.2,<5      # ChatGoogleGenerativeAI
langchain-chroma>=1.1,<2            # Chroma vector store integration

# --- LLM / embeddings provider (current SDK; used for Vision map extraction) ---
google-genai>=1.0.0

# --- Vector store + keyword search ---
chromadb>=0.5.0
rank-bm25>=0.2.2                     # BM25 keyword retriever for hybrid search

# --- Reranking (local cross-encoder) ---
sentence-transformers>=3.0.0        # BAAI/bge-reranker-base

# --- PDF parsing ---
pypdf>=5.0.0
pdfplumber>=0.11.0

# --- Web app (frontend + API) ---
fastapi>=0.115
uvicorn[standard]>=0.30

# --- Utilities ---
python-dotenv>=1.0.0
tqdm>=4.66.0
